FROM golang:1.25-alpine

WORKDIR /app

# Install air for hot-reload (v1.65+ requires Go 1.25)
RUN go install github.com/air-verse/air@latest

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 8080

CMD ["air", "-c", ".air.toml"]
